on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: 'after set up'
        run: ls
      - name: 'login docker'
        run: docker login --username hendhany --password dckr_pat_28xatMCkLxM8dZH36cGDoVn8ltc  